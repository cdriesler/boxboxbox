class Program {
  final String name;
  int quota;
  double w;
  double h;

  Program(this.name, this.quota) {
    //w = new_width == null ? null : new_width;
    //h = new_height == null ? null : new_height;
  }
}